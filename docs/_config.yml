# Jekyll configuration for GitHub Pages
title: AIProxyGuard
description: LLM Security Proxy with Prompt Injection Detection
baseurl: "/aiproxyguard"
url: "https://ainvirion.github.io"

# Theme
remote_theme: carlosperate/jekyll-theme-rtd

# Build settings
markdown: kramdown
highlighter: rouge

# Navigation is controlled via front matter (nav_order, has_children, parent)
